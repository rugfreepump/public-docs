---
title: "Anchor IDL & Types"
description: "Access the published Anchor IDL and TypeScript types for the RugFreePump on-chain program."
---

RugFreePump publishes its **Anchor IDL** and **TypeScript types** as the machine-readable representation of its public on-chain program interface.

* **Anchor IDL:** Describes the program interface, including instructions, account layouts, arguments, events, errors, and defined types.
* **TypeScript Types:** Provides the generated TypeScript representation of the IDL for strongly typed client integrations.

RugFreePump maintains the same program interface across its supported Devnet and Mainnet deployments. The published IDL and TypeScript types therefore represent the shared protocol interface rather than separate cluster-specific variants.

## Published Artifacts

The official integration artifacts are maintained in the RugFreePump repository:

| Artifact             | Source File                                                                                                   | Description                                                             |
| :------------------- | :------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------------- |
| **Anchor IDL**       | [`rug_free_pump.json`](https://github.com/rugfreepump/public-docs/blob/main/idl-and-types/rug_free_pump.json) | Machine-readable description of the RugFreePump program interface.      |
| **TypeScript Types** | [`rug_free_pump.ts`](https://github.com/rugfreepump/public-docs/blob/main/idl-and-types/rug_free_pump.ts)     | Generated TypeScript representation of the published program interface. |

## Supported Deployments

RugFreePump maintains separate Devnet and Mainnet deployments while keeping their program interfaces synchronized.

When a program update changes the interface, the corresponding program version is deployed across the supported clusters and the published IDL and TypeScript types are updated accordingly.

| Deployment Cluster | Solana Cluster Target | RugFreePump Program ID                         |
| :----------------- | :-------------------- | :--------------------------------------------- |
| **Devnet**         | `devnet`              | `9hpBPEiywp7FPAq5uP253eAZzdJ1Cpd36L5RAYuc9xSw` |
| **Mainnet**        | `mainnet-beta`        | `6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P`  |

> **Deployment note:** The program ID identifies a deployment and is independent of the program interface. If the program is deployed under a different address without changing its interface, the corresponding deployment address can be updated independently without requiring separate IDL or TypeScript artifacts.

## Interface Synchronization & Versioning

The published IDL and generated TypeScript types are updated when changes to the program interface require the published artifacts to change.

Examples include:

* **Instructions:** Changes to public instruction definitions or entry points.
* **Arguments:** Changes to instruction arguments or their types.
* **Accounts:** Changes to account structures or account relationships exposed by the interface.
* **Events:** Changes to program-emitted event definitions.
* **Errors and Types:** Changes to custom errors or defined types represented in the IDL.

Devnet and Mainnet are maintained against the same program interface so developers can use Devnet to test integrations against the interface intended for the production deployment.

## Integration Reference

* **Detailed Reference:** For transaction construction and exact account requirements, see the [Buy Instruction](../on-chain-integration/buy) documentation.
* **Complete Schema:** For the complete machine-readable program interface, use the published [Anchor IDL](https://github.com/rugfreepump/public-docs/blob/main/idl-and-types/rug_free_pump.json).
