---
title: "Anchor IDL & Types"
description: "Access the published Anchor IDL and TypeScript types for the RugFreePump on-chain program."
---

RugFreePump publishes its **Anchor IDL** and **TypeScript types** as the machine-readable representation of its public on-chain program interface.

* **Anchor IDL:** Describes the program interface, including instructions, account layouts, arguments, events, errors, and defined types.
* **TypeScript Types:** Provides the generated TypeScript representation of the IDL for strongly typed client integrations.

RugFreePump maintains the identical program interface across its supported Devnet and Mainnet deployments. The published IDL and TypeScript types represent a shared protocol interface rather than separate, cluster-specific variants.

## Published Artifacts

The official integration artifacts are maintained in the RugFreePump repository:

| Artifact             | Source File                                                                                                   | Description                                                             |
| :------------------- | :------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------------- |
| **Anchor IDL**       | [`rug_free_pump.json`](https://github.com/rugfreepump/public-docs/blob/main/idl-and-types/rug_free_pump.json) | Machine-readable description of the RugFreePump program interface.      |
| **TypeScript Types** | [`rug_free_pump.ts`](https://github.com/rugfreepump/public-docs/blob/main/idl-and-types/rug_free_pump.ts)     | Generated TypeScript representation of the published program interface. |

## Supported Deployments

RugFreePump maintains separate Devnet and Mainnet deployments while keeping their underlying program interfaces strictly synchronized.

The program ID may differ between deployments, but the published IDL and TypeScript types uniformly map to both environments.

| Deployment Cluster | Solana Cluster Target | RugFreePump Program ID                         |
| :----------------- | :-------------------- | :--------------------------------------------- |
| **Devnet**         | `devnet`              | `9hpBPEiywp7FPAq5uP253eAZzdJ1Cpd36L5RAYuc9xSw` |
| **Mainnet**        | `mainnet-beta`        | `6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P`  |

<Callout type="info" title="Deployment Address Parity">

A Program ID identifies a specific deployment and operates independently of the interface layout. Updating a cluster deployment address does not require new IDL or TypeScript artifacts if the underlying program interface remains unchanged.

</Callout>

## Interface Synchronization & Versioning

The published IDL and generated TypeScript types serve as the live reference for the current RugFreePump program interface.

<Callout type="warning" title="Last Updated">

**September 22, 2026**

This date marks the latest published state of the IDL and TypeScript artifacts. Developers should use this date as the baseline reference when determining whether their local implementation matches the current published interface.

</Callout>

Interface updates are made when program modifications alter the published interface. Examples include:

* **Instructions:** Changes to public instruction definitions or entry points.
* **Arguments:** Modifications to instruction parameters or their data types.
* **Accounts:** Alterations to account schemas or structural relationships exposed by the interface.
* **Events:** Adjustments to program-emitted event definitions.
* **Errors and Types:** Additions or changes to custom errors and defined types represented in the IDL.

RugFreePump does not intentionally introduce breaking changes to its public interface. However, exceptional protocol upgrades may require structural modifications. If a change affects the published IDL or TypeScript types, the updated artifacts and revised **Last Updated** date will be published here.

Devnet and Mainnet remain synchronized to the same current program interface, allowing developers to use Devnet to test against the interface intended for the production deployment.

<Callout type="warning" title="Use the Current Artifacts">

The IDL and TypeScript files linked on this page are the authoritative published artifacts for the active RugFreePump interface. When maintaining a production integration, verify the current **Last Updated** date above and do not rely on an older cached or local copy when a newer version has been published.

</Callout>
