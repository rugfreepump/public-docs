---
title: "Anchor IDL & Types"
description: "Access the published Anchor IDL and generated TypeScript types for the RugFreePump on-chain program."
---

RugFreePump publishes its Anchor IDL and generated TypeScript types as the machine-readable representation of its public on-chain program interface.

The **Anchor IDL** describes the program interface, including its instructions, accounts, arguments, events, and defined types.

The **generated TypeScript types** provide the corresponding TypeScript type representation for client integrations.

RugFreePump maintains the same program interface across its supported Devnet and Mainnet deployments. The published IDL and generated TypeScript types therefore represent the shared RugFreePump interface rather than separate cluster-specific interfaces.

## Published Artifacts

The current integration artifacts are:

| Artifact             | Description                                         |
| :------------------- | :-------------------------------------------------- |
| `rug_free_pump.json` | Published Anchor IDL for the RugFreePump program    |
| `rug_free_pump.ts`   | Generated TypeScript type representation of the IDL |

### Anchor IDL

The Anchor IDL is the canonical machine-readable description of the RugFreePump program interface.

[View `rug_free_pump.json`](https://raw.githubusercontent.com/rugfreepump/public-docs/main/developer-guide/anchor-idl-and-types/resources/rug_free_pump.json)

### Generated TypeScript Types

The generated TypeScript file provides the TypeScript representation of the published IDL for use by TypeScript integrations.

[View `rug_free_pump.ts`](https://raw.githubusercontent.com/rugfreepump/public-docs/main/developer-guide/anchor-idl-and-types/resources/rug_free_pump.ts)

## Supported Deployments

RugFreePump maintains both Devnet and Mainnet deployments of the protocol.

The deployments are maintained against the same program interface. When the protocol implementation is updated, the corresponding program changes are applied across the supported clusters so developers can test against the same interface intended for production use.

| Deployment  | Solana Cluster | Program ID                                     |
| :---------- | :------------- | :--------------------------------------------- |
| **Devnet**  | `devnet`       | `9hpBPEiywp7FPAq5uP253eAZzdJ1Cpd36L5RAYuc9xSw` |
| **Mainnet** | `mainnet-beta` | `9hpBPEiywp7FPAq5uP253eAZzdJ1Cpd36L5RAYuc9xSw` |

The program ID shown for Mainnet represents the currently documented deployment value. If the production deployment uses a different program ID, the Mainnet address will be updated independently without requiring a separate IDL or TypeScript artifact when the program interface remains unchanged.

## Interface Synchronization

The published IDL and generated TypeScript types are maintained together with the RugFreePump program interface.

Interface changes that affect the published artifacts may include changes such as:

* Instruction definitions
* Instruction arguments
* Account structures
* Account relationships
* Events
* Defined types

When such changes are introduced, the published IDL and generated TypeScript types are updated to reflect the new program interface.

Devnet and Mainnet are kept synchronized with the corresponding program interface so developers can use Devnet to test against the same protocol interface used by the production deployment.

A change to the program address alone does not constitute an interface change and does not require a separate set of IDL or TypeScript artifacts.

## Integration Reference

The [Buy](../on-chain-integration/buy) reference provides the detailed requirements for the currently documented public integration entry point.

For the complete machine-readable interface, use the published Anchor IDL above.
